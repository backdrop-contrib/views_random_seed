type = module
name = Views random seed
description = "Adds a random order handler with fixed seed to consistently order random listings avoid duplicates across pages and displays."
package = Views
backdrop = 1.x
dependencies[] = views

